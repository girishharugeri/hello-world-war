pipeline {
  agent none
  stages {
    stage ( 'my build' ) {
      agent { label 'master' }
      steps {
          sh 'whoami'
          sh 'ls'
          sh 'pwd'
          sh 'mvn package'
          sh 'scp -R /target/hello-world-war-1.0.0 root@172.31.12.7:/opt/tomcat/webapps'
       }
     }  
          stage ( 'my deploy' ) {
            agent { label 'slave_jenkins' }
              steps { 
                sh 'sudo tomcatdown'
                sh 'sudo tomcatup'
      }   
    }
  }
}
